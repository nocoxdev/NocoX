import type { Tab } from 'rc-tabs/lib/interface';
import MiddlePropWidget from '../../components/MiddlePropWidget';

const TabPanelView = () => {
  return MiddlePropWidget<Tab>;
};

export default TabPanelView;
