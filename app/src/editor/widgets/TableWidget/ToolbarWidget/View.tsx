import type { FlexViewProps } from '../../FlexWidget/View';
import FlexView from '../../FlexWidget/View';

const TableToolbarView = (props: FlexViewProps) => {
  return <FlexView {...props} />;
};

export default TableToolbarView;
