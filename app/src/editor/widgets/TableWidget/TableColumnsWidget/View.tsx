export interface TableColumnsProps {
  children?: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
}
const TableColumnsView = (_: TableColumnsProps) => {
  return null;
};

export default TableColumnsView;
