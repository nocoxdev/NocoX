import UserList from '@/editor/ui/Setting/RoleList';
import { StyledContainer } from './styled';

const VisitControl = () => {
  return (
    <StyledContainer>
      <UserList />
    </StyledContainer>
  );
};

export default VisitControl;
