import type { IconPickerProps } from '@/components/IconPicker';
import IconPicker from '@/components/IconPicker';

const IconInput = (props: IconPickerProps) => {
  return <IconPicker {...props} />;
};

export default IconInput;
